from __future__ import annotations

from typing import Any


def compare_research_results(_legacy: dict[str, Any], _langgraph: dict[str, Any]) -> dict[str, Any]:
    """Placeholder for the structured parity comparator.

    Slice 0A only establishes the module boundary. Real comparator semantics
    are introduced when golden-set parity work begins.
    """

    return {"status": "not_implemented", "slice": "0a"}
