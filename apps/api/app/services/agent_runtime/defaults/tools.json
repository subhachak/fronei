[
  {
    "id": "answer_directly",
    "name": "answer_directly",
    "description": "Generate a normal chat answer from the current goal and context.",
    "input_schema": {"query": "str", "context": "dict"},
    "output_schema": {"answer": "str", "model_used": "str"},
    "allowed_agent_ids": ["direct_answer_agent", "orchestrator"],
    "guardrail_policy_ids": [],
    "timeout_ms": 30000,
    "retry_policy": {"max_attempts": 1},
    "idempotent": true,
    "backend": "native",
    "backend_ref": "turn_graph.answer_directly",
    "enabled": true,
    "version": "1.0.0"
  },
  {
    "id": "web_search",
    "name": "web_search",
    "description": "Search the public web for current information.",
    "input_schema": {"query": "str", "recency": "Optional[str]", "max_results": "int"},
    "output_schema": {"sources": "list[WebSource]", "provider": "str"},
    "allowed_agent_ids": ["source_scout", "direct_answer_agent", "research_lead"],
    "guardrail_policy_ids": ["tool.ssrf_prevention", "tool.output_sanitize"],
    "timeout_ms": 15000,
    "retry_policy": {"max_attempts": 2, "backoff_ms": 500},
    "idempotent": true,
    "backend": "mcp",
    "backend_ref": "mcp.web.search",
    "enabled": true,
    "version": "1.0.0"
  },
  {
    "id": "read_url",
    "name": "read_url",
    "description": "Extract readable content from a known public URL.",
    "input_schema": {"url": "str"},
    "output_schema": {"content": "str", "url": "str", "provider": "str"},
    "allowed_agent_ids": ["source_reader", "direct_answer_agent", "research_lead"],
    "guardrail_policy_ids": ["tool.ssrf_prevention", "tool.output_sanitize"],
    "timeout_ms": 15000,
    "retry_policy": {"max_attempts": 2, "backoff_ms": 500},
    "idempotent": true,
    "backend": "mcp",
    "backend_ref": "mcp.web.search",
    "enabled": true,
    "version": "1.0.0"
  },
  {
    "id": "generate_document",
    "name": "generate_document",
    "description": "Generate structured document or presentation content.",
    "input_schema": {"goal": "Goal", "document_brief": "dict"},
    "output_schema": {"document_plan": "dict"},
    "allowed_agent_ids": ["document_lead", "content_strategist", "orchestrator"],
    "guardrail_policy_ids": ["document.template_ownership"],
    "timeout_ms": 60000,
    "retry_policy": {"max_attempts": 1},
    "idempotent": false,
    "backend": "native",
    "backend_ref": "documents.generate_document_output",
    "enabled": true,
    "version": "1.0.0"
  }
]
